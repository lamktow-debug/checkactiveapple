"""Phiên bản của app.

Script đóng gói đọc và ghi đúng file này, nên đừng đổi định dạng dòng dưới.
Quy ước: MAJOR.MINOR.PATCH — tag trên GitHub là "v" + số này (vd v1.2.0).
"""

__version__ = "1.4.2"
